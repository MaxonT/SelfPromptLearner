# Render Keep-Alive 设置指南

## 🎯 目标

防止你的 Render.com 免费版应用在 15 分钟不活动后进入睡眠状态。

## 📋 问题说明

Render 免费版应用会在以下情况睡眠：
- ⏰ **15 分钟**没有收到任何 HTTP 请求
- 😴 睡眠后，下次访问时需要**冷启动**（可能需要 30-60 秒）

## ✅ 解决方案

使用 **GitHub Actions** 每 10 分钟自动 ping 你的应用，保持它处于活跃状态。

---

## 🚀 设置步骤

### 步骤 1: 获取你的 Render 应用 URL

1. 登录 [Render Dashboard](https://dashboard.render.com)
2. 找到你的应用（例如：`spr-fullstack`）
3. 复制应用 URL（格式：`https://你的应用名.onrender.com`）

**示例**: `https://spr-fullstack.onrender.com`

### 步骤 2: 在 GitHub 添加 Secret

1. 打开你的 GitHub 仓库
2. 点击 **Settings** → **Secrets and variables** → **Actions**
3. 点击 **New repository secret**
4. 添加以下 secret：
   - **Name**: `RENDER_APP_URL`
   - **Value**: `https://你的应用名.onrender.com`（你在步骤 1 复制的 URL）
5. 点击 **Add secret**

### 步骤 3: 推送代码

```bash
git add .
git commit -m "添加 Render Keep-Alive 配置"
git push origin main
```

### 步骤 4: 验证 GitHub Actions

1. 在 GitHub 仓库中，点击 **Actions** 标签
2. 找到 **Render Keep-Alive** workflow
3. 点击 **Run workflow** → **Run workflow** 手动触发一次测试
4. 查看运行日志，应该看到：
   ```
   ✅ Ping 成功! 端点: /api/health
   ```

### 步骤 5: 删除旧的 Streamlit Keep-Alive（可选）

如果不再需要 Streamlit 的配置：

```bash
rm .github/workflows/streamlit-keep-alive.yml
rm keep_alive.py
rm streamlit_keep_alive.py
git add .
git commit -m "删除旧的 Streamlit keep-alive 配置"
git push
```

---

## 🔍 验证运行状态

### 方法 1: 查看 GitHub Actions 日志

1. 进入 **GitHub Actions** 标签
2. 点击最新的 **Render Keep-Alive** 运行记录
3. 查看日志，应该看到：
   ```
   ⏱️  [2026-02-10 10:00:00] Pinging https://你的应用.onrender.com/api/health...
   📊 状态码: 200
   ✅ Ping 成功! 端点: /api/health
   ```

### 方法 2: 检查运行频率

- GitHub Actions 会**每 10 分钟**自动运行一次
- 你可以在 **Actions** 标签看到运行历史
- 应该看到规律的运行记录（每 10 分钟一次）

### 方法 3: 测试应用是否保持活跃

1. 等待 20-30 分钟不访问你的应用
2. 然后访问 `https://你的应用.onrender.com`
3. 应该**立即**加载，没有冷启动延迟

---

## 📊 运行时间表

| 时间 | 动作 | 状态 |
|------|------|------|
| 10:00 | GitHub Actions Ping | ✅ 应用保持活跃 |
| 10:10 | GitHub Actions Ping | ✅ 应用保持活跃 |
| 10:20 | GitHub Actions Ping | ✅ 应用保持活跃 |
| 10:30 | GitHub Actions Ping | ✅ 应用保持活跃 |

---

## 🛠️ 本地测试（可选）

如果想在本地测试 keep-alive 脚本：

```bash
# 设置环境变量
export RENDER_APP_URL="https://你的应用名.onrender.com"

# 安装依赖
pip install requests

# 运行脚本
python render_keep_alive.py
```

输出示例：
```
============================================================
🚀 Render Keep-Alive 脚本启动
============================================================
📍 目标应用: https://spr-fullstack.onrender.com
⏰ Ping 间隔: 600 秒 (10 分钟)
💡 提示: Render 免费版应用会在 15 分钟不活动后睡眠
============================================================

[2026-02-10 10:00:00] 开始 ping...
✅ Ping 成功 (/api/health) - 状态码: 200
⏳ 等待 600 秒后再次 ping...
```

---

## ❓ 常见问题

### Q: 为什么选择 10 分钟间隔？

A: Render 免费版在 **15 分钟**不活动后睡眠，10 分钟间隔提供了安全边界。

### Q: GitHub Actions 有使用限制吗？

A: GitHub 免费账户每月有 **2000 分钟**的 Actions 配额。这个 keep-alive 每月使用约 **150 分钟**，完全在免费额度内。

### Q: 为什么不使用 5 分钟间隔？

A: 10 分钟已经足够，使用更短的间隔会浪费 GitHub Actions 配额且没有额外好处。

### Q: 如果我的应用还是睡着了怎么办？

A: 检查：
1. GitHub Secret `RENDER_APP_URL` 是否正确设置
2. GitHub Actions 是否成功运行（查看 Actions 标签）
3. Render 应用的 `/api/health` 端点是否正常工作

---

## 🎉 完成！

设置完成后，你的 Render 应用将保持活跃状态，用户访问时不会遇到冷启动延迟！

### 下一步
- 监控 GitHub Actions 运行日志
- 定期检查应用响应速度
- 如有问题，查看 Actions 日志排查
