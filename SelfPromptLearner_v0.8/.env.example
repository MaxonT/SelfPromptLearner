# ==============================
# Self-Prompt Reflection (SPR)
# Environment Variables Example
# ==============================

# --- Server ---
PORT=5000

# --- Database (Postgres) ---
# Option A (recommended for local dev): Docker Compose (see docker-compose.yml)
# DATABASE_URL=postgres://spr:spr_password@localhost:5432/spr

# Option B: Managed Postgres (Neon / Supabase / Render)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# --- Sessions (future Stage: auth) ---
# SESSION_SECRET=replace_me

# Production (required for Stage 6+)
SESSION_SECRET=
APP_ORIGIN=http://localhost:5000
PORT=5000
