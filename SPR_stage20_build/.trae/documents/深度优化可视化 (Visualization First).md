1. **重构布局**：引入更专业的 Dashboard 布局（KPI 卡片置顶、图表分块、Tab 分页）。
2. **增强图表**：

   * **关键词趋势**：不只是词云，加一个「时间轴」，看你上个月关注 Python，这个月是不是转去关注 Rust 了。

   * **交互式过滤**：点击雷达图上的「编程」，下面只显示编程类的 Prompt 列表。

   * **Prompt 详情表**：增加一个可搜索、可排序的数据表格，不再只是看统计图，要能看到具体哪条 Prompt 写得好。
3. **引入 Plotly**：替换 Matplotlib，让图表可缩放、鼠标悬停显示数值，交互性提升 10 倍。

**执行顺序：**

1. 安装 `plotly` 库。
2. 修改 `prompt_mirror.py`，用 `plotly.express` 重写雷达图和柱状图。
3. 增加「原始数据浏览」Tab 页。

